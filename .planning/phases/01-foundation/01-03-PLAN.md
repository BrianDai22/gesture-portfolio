---
phase: 01-foundation
plan: 03
type: execute
---

<objective>
Create HTML scaffold with CDN imports, base CSS styles, and JavaScript entry point.

Purpose: Establish the foundational files that all features will build upon - verified working in browser.
Output: index.html with Three.js + MediaPipe loaded via CDN, base CSS with minimalist dark theme, main.js module entry point.
</objective>

<execution_context>
@~/.claude/plugins/marketplaces/taches-cc-resources/skills/create-plans/workflows/execute-phase.md
@~/.claude/plugins/marketplaces/taches-cc-resources/skills/create-plans/templates/summary.md
@~/.claude/plugins/marketplaces/taches-cc-resources/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<important>
Use Context7 MCP to look up current CDN URLs and best practices for:
- Three.js CDN import (ES module version)
- MediaPipe Hands CDN import
- ES module patterns for vanilla JS projects
</important>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html with CDN imports</name>
  <files>index.html</files>
  <action>
    Create index.html with:

    1. DOCTYPE and semantic HTML5 structure
    2. Meta tags (charset, viewport, description, author)
    3. Title: "Brian Dai | Gesture Portfolio"
    4. Link to styles/main.css
    5. CDN imports for Three.js (ES module version from esm.sh or unpkg)
    6. CDN imports for MediaPipe Hands
    7. Script tag for src/js/main.js with type="module"

    Structure should include:
    - <main> element for Three.js canvas
    - <div id="ui-overlay"> for gesture tutorial and navigation hints
    - <video id="camera-feed"> (hidden, for MediaPipe input)
    - <canvas id="three-canvas"> for Three.js rendering

    Use Context7 to verify correct CDN URLs for Three.js and MediaPipe.

    IMPORTANT: Use ES module imports, not global script tags where possible.
  </action>
  <verify>
    - index.html is valid HTML5 (check with validator or visual inspection)
    - All required elements present (main, canvas, video, overlay div)
    - CDN script tags have correct URLs
  </verify>
  <done>index.html created with proper structure and CDN imports</done>
</task>

<task type="auto">
  <name>Task 2: Create base CSS with minimalist dark theme</name>
  <files>styles/main.css</files>
  <action>
    Create main.css with:

    1. CSS Reset (minimal - box-sizing, margin/padding reset)
    2. CSS Custom Properties (variables) for theming:
       - --color-bg: dark background (#0a0a0a or similar)
       - --color-text: light text (#f5f5f5)
       - --color-accent: subtle accent (muted blue or cyan)
       - --font-primary: clean sans-serif (system font stack)
       - --font-mono: monospace for code/tech feel
       - --spacing-* units

    3. Base styles:
       - Body: full viewport, dark background, light text
       - Typography: clean, readable, minimal
       - Canvas: full viewport, fixed position
       - UI overlay: positioned over canvas, pointer-events managed
       - Hidden video element styles

    4. Utility classes:
       - .hidden { display: none }
       - .visually-hidden (for accessibility)

    Keep it minimal - this is foundation only. More styles added in Phase 3.
  </action>
  <verify>
    - main.css exists with CSS variables defined
    - No syntax errors (browser dev tools won't show CSS errors)
    - Dark theme colors properly set
  </verify>
  <done>Base CSS created with minimalist dark theme and CSS variables</done>
</task>

<task type="auto">
  <name>Task 3: Create JavaScript entry point</name>
  <files>src/js/main.js</files>
  <action>
    Create main.js as the ES module entry point:

    1. Import statement placeholders for Three.js (commented, will activate in Phase 2)
    2. Import statement placeholders for MediaPipe (commented, will activate in Phase 2)
    3. DOM content loaded listener
    4. Console log to verify script loads: "Gesture Portfolio initialized"
    5. Basic module structure with init() function
    6. Export init function for potential testing

    Structure:
    ```javascript
    // Three.js and MediaPipe imports will be added in Phase 2
    // import * as THREE from 'three';
    // import { Hands } from '@mediapipe/hands';

    const init = () => {
      console.log('Gesture Portfolio initialized');
      // Phase 2: Camera setup
      // Phase 2: Three.js scene setup
      // Phase 2: MediaPipe hands setup
    };

    document.addEventListener('DOMContentLoaded', init);

    export { init };
    ```

    Keep minimal - this proves ES modules work. Real implementation in Phase 2.
  </action>
  <verify>
    - main.js exists with module structure
    - No syntax errors (test by loading in browser)
    - Console shows initialization message
  </verify>
  <done>JavaScript entry point created with module structure</done>
</task>

<task type="auto">
  <name>Task 4: Commit foundation files</name>
  <files>index.html, styles/main.css, src/js/main.js</files>
  <action>
    Stage all new files: `git add index.html styles/ src/`
    Commit: "feat: add HTML scaffold, base CSS, and JS entry point"
    Push: `git push`
  </action>
  <verify>
    - `git status` shows clean working directory
    - GitHub shows all new files
  </verify>
  <done>Foundation files committed and pushed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete foundation: HTML with CDN imports, dark theme CSS, JS entry point</what-built>
  <how-to-verify>
    1. Run: `npm run dev` (starts live-server)
    2. Browser should open to http://localhost:3000
    3. Verify: Page has dark background (not white)
    4. Open DevTools Console (F12 or Cmd+Option+I)
    5. Verify: Console shows "Gesture Portfolio initialized"
    6. Verify: No red errors in Console (CDN imports may show as unused, that's OK)
    7. Verify: Network tab shows Three.js and MediaPipe scripts loaded (200 status)
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] index.html loads without errors
- [ ] Dark theme visible (not default white)
- [ ] Console shows initialization message
- [ ] CDN scripts load (check Network tab)
- [ ] ES modules working (no CORS or module errors)
- [ ] All files committed and pushed to GitHub
</verification>

<success_criteria>
- Foundation files created and working
- Dark minimalist theme applied
- CDN imports loading correctly
- JavaScript module system functional
- Phase 1 complete, ready for Phase 2 (Core Interaction)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`

This summary should note:
- Phase 1: Foundation is COMPLETE
- All 3 plans executed successfully
- Ready for Phase 2: Core Interaction
</output>
