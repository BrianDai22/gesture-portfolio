---
phase: 01-foundation
plan: 02
type: execute
---

<objective>
Set up development server and npm dependencies for local development workflow.

Purpose: Enable hot-reload development with live-server for rapid iteration.
Output: Working dev server with npm scripts, verified to run correctly.
</objective>

<execution_context>
@~/.claude/plugins/marketplaces/taches-cc-resources/skills/create-plans/workflows/execute-phase.md
@~/.claude/plugins/marketplaces/taches-cc-resources/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize npm and install live-server</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run `npm init -y` to create package.json.

    Update package.json with proper metadata:
    - name: "gesture-portfolio"
    - description: "A camera-based, gesture-controlled portfolio website"
    - author: "Brian Dai"
    - license: "MIT"
    - type: "module" (for ES modules support)

    Install live-server as dev dependency:
    `npm install --save-dev live-server`

    This is a lightweight dev server with auto-reload - no bundler needed.
  </action>
  <verify>
    - package.json exists with correct metadata
    - package.json has "type": "module"
    - node_modules/live-server exists
    - package-lock.json exists
  </verify>
  <done>npm initialized with live-server installed</done>
</task>

<task type="auto">
  <name>Task 2: Configure npm scripts</name>
  <files>package.json</files>
  <action>
    Add scripts to package.json:
    ```json
    "scripts": {
      "dev": "live-server --port=3000 --open",
      "start": "live-server --port=3000"
    }
    ```

    - `npm run dev`: Opens browser automatically
    - `npm start`: Runs server without opening browser

    Test that live-server starts (will show directory listing since no index.html yet).
  </action>
  <verify>
    - `npm run start` starts server on port 3000 (then Ctrl+C to stop)
    - No errors in terminal output
  </verify>
  <done>npm scripts configured and tested</done>
</task>

<task type="auto">
  <name>Task 3: Commit and push changes</name>
  <files>package.json, package-lock.json</files>
  <action>
    Stage changes: `git add package.json package-lock.json`
    Commit: "chore: add dev server setup with live-server"
    Push: `git push`

    Note: node_modules should be ignored via .gitignore from previous plan.
  </action>
  <verify>
    - `git status` shows clean working directory
    - `git log` shows new commit
    - GitHub shows updated package.json
  </verify>
  <done>Dev server setup committed and pushed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts live-server on port 3000
- [ ] Server auto-reloads on file changes (will test fully in next plan)
- [ ] package.json has correct scripts and "type": "module"
- [ ] Changes committed and pushed to GitHub
</verification>

<success_criteria>
- live-server installed and working
- npm scripts configured (dev, start)
- ES modules enabled in package.json
- Ready for next plan (HTML/CSS scaffold)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
